import { HrLettersLayout } from "@/components/hrletters/HrLettersLayout";

export default function HrLettersRootLayout({ children }: { children: React.ReactNode }) {
  return <HrLettersLayout>{children}</HrLettersLayout>;
}
