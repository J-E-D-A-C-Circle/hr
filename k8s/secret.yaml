apiVersion: v1
kind: Secret
metadata:
  name: hr-validation-secret
  namespace: hr-validation
type: Opaque
stringData:
  NEXTAUTH_SECRET: "change-this-to-a-secure-random-secret-in-production"
  # Optional: If migrating to PostgreSQL/MySQL in Kubernetes:
  # DATABASE_URL: "postgresql://user:password@postgres-service:5432/hr_db"
